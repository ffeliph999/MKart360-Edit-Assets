#ifndef MK64_XBOX360_OBJBASE_SHIM_H
#define MK64_XBOX360_OBJBASE_SHIM_H

/*
 * Xbox 360 XDK compatibility shim.
 * The desktop Windows SDK ObjBase.h is not valid for the Xenon/PowerPC target.
 * XDK headers already provide the COM/basic types D3DX needs before including
 * this file, so this header intentionally stays minimal.
 */

#endif
